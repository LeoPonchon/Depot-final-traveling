{
  "name": "traveling-functions",
  "description": "FCM push on new photos (subscription rules) and on new moderation reports (admins)",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "private": true
}
